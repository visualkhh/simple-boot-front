export class UriUtils {
    // static decode(uri: string): string {
    //     return decodeURIComponent(uri);
    // }
    // static encode(uri: string): string {
    //     return encodeURIComponent(uri);
    // }
    //
    // static decodeObjectProperty(obj: any) {
    //     const n = Object.assign({}, obj);
    //     for (const key in n) {
    //         if (n.hasOwnProperty(key)) {
    //             n[key] = UriUtils.decode(n[key]);
    //         }
    //     }
    //     return n;
    // }
    // static encodeObjectProperty(obj: any) {
    //     const n = Object.assign({}, obj);
    //     for (const key in n) {
    //         if (n.hasOwnProperty(key)) {
    //             n[key] = UriUtils.encode(n[key]);
    //         }
    //     }
    //     return n;
    // }
}
